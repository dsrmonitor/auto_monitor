version: '2'
services:
    frotainteligente-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/FrotaInteligente/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=FrotaInteligente
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
